# 人才简历智能筛选与管理系统

基于 LangChain + LangGraph + FastAPI 的智能简历筛选平台。

## 技术栈

- Python 3.13+
- FastAPI + LangChain + LangGraph
- MySQL + MinIO + Redis + ChromaDB
- Streamlit 前端

## 快速开始

```bash
# 安装依赖
uv sync

# 启动服务
uv run uvicorn src.api.main:app --reload
```
